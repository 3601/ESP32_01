import time

class SwitchFactory:
    def __init__():
        self.switches = {}
        self.timers = {}
"""
    def add_node(name, func, interval = False, state = False):
        if interval:
            self.timers[name] = {'func' : func,
            'interval': interval,
            'la'}

            [func, interval, 0.0]
        else:
            self.switches[name] = [func, state]

    def loop():




                try:
                    self.client.check_msg()
                    duration = time.time() - self.last_msg
                    if self.nodes and (duration > self.mqtt_set['msg_interval']):
                        self.publish_nodes()
                        self.last_msg = time.time()
                except OSError as e:
                    self._restart_and_connect()

"""
